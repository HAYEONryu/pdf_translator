# 사내 기술문서 번역 도구 — 구현 지시서 (v4.1 / 1차 개발)

> Claude Code 작업용. Step 0 스파이크(§0-A) 결과를 반영한 v4, 이후 실사용 중 발견한
> 문제들을 반영한 v4.1.
> v3 대비 변경점은 [부록: v3→v4 변경 이력](#부록-v3v4-변경-이력),
> v4 대비 변경점은 [부록: v4→v4.1 변경 이력](#부록-v4v41-변경-이력) 참고.
>
> **1차 범위:** 로그인·DB·보안 심사는 **전부 제외**합니다.
> 상태는 전부 **파일시스템**에 둡니다. 구조를 최대한 단순하게 유지하세요.
> (로그인·DB·보안은 2차 고도화 예정 — 다만 지금 그 자리를 미리 만들어두지는 마세요.)

---

## 0. 역할·목표·행동 규칙

**목표:** 사내 구성원이 외국어 기술문서(PDF)를 **링크 하나로 접속해** 번역하고, **원문과 좌우 대조로 읽는** 웹 도구.

사용자는 "옆자리 동료"입니다. 설치·계정·설정 없이 URL만으로 써야 하고, 기술문서의 **표·수치·규격번호가 깨지지 않아야** 합니다.

### 행동 규칙 (엄수)

1. **논리적 모순을 발견하면 즉시 중단하고 보고하세요.** 지시서 내 상충 요구사항이나 구현 중 드러난 설계 결함을 발견하면 **코드를 계속 쓰지 말고** 무엇이 왜 모순인지 설명한 뒤 지시를 기다리세요.
2. **§1 확정 사항 임의 변경 금지.** 변경 필요 시 근거와 함께 먼저 질문.
3. **단계 순서 준수** (§10). 역순 진행 금지.
4. **2차용 코드를 미리 만들지 마세요.** 사용자 모델, DB 추상화 레이어, 권한 데코레이터, 인증 스텁 전부 금지. 쓰지 않는 추상화가 1차를 느리게 만듭니다.
5. 추측으로 채우지 말고 질문하세요.

### 0-A. Step 0 스파이크 결과 요약 (2026-08-19, samplePDF.pdf 기준)

- **ⓐ 표 재구성 품질:** 우수. 병합 헤더·중첩 컬럼(26행×8열)에서 행/열 100% 보존, 숫자 셀 손상 0건, 헤더는 원문 병기 규칙대로 번역됨. `gpt-5.6-luna`로 충분히 검증됨.
- **ⓑ bbox 좌표 정확도:** 정확. `bbox × render_scale` 오버레이가 실제 렌더링과 픽셀 단위로 일치. y축 반전 없음.
- **ⓒ 표-텍스트 중복:** 순수 데이터 표(46행×5열)에서는 제외 로직 정상(189개 단어 중 184개 정확히 제외). 그러나 **차트/그래프가 `find_tables()`에 의해 표로 오탐지되는 사례 발견** — 오탐지된 표의 셀은 대부분 빈 값이었고, 차트의 축 눈금·범례가 표 bbox 바깥(안전 마진 없음)에 위치해 무의미한 "paragraph" 블록으로 새어나감. → §5.1에 표/figure 후보 검증·마진 확장 파이프라인 추가로 대응(§4, §5.1 참고).
- **부수 발견:** 샘플 문서가 독일어라 숫자가 `71,36`(콤마=소수점) 표기 — 기존 §5.4 정규화("천단위 콤마 제거")를 그대로 적용하면 수치가 깨짐. → §5.4에 언어 무관 정규화 + 언어 범위 제한(영어 표기만 검증, 범위 밖은 검증 스킵) 추가.

---

## 1. 확정 기술 스택 (변경 금지)

| 영역 | 선택 |
|---|---|
| 백엔드 | **FastAPI** (Python 3.11) |
| 템플릿 | **Jinja2 서버렌더** |
| 프론트 | **Vanilla JS** (클릭 대조 + SSE만) |
| 저장소 | **파일시스템만.** DB·ORM 금지 |
| PDF | **pdfplumber**(텍스트·bbox·표) + **PyMuPDF**(렌더링) |
| LLM | **OpenAI API** (모델 §5.3) |
| 서버 | **uvicorn `--workers 1`** (이유 §6) |

**금지:** SQLAlchemy·Alembic·SQLite, 로그인/세션/bcrypt, Tesseract/OCR, Streamlit, React/Vue, Redis

> 로컬 개발 환경 Python이 3.14.6으로 확인되었습니다(spike 진행 시). §1의 3.11 고정 여부를 실제 venv 구성 시 재확인하세요 — 3.11이 없으면 어떤 버전으로 진행할지 먼저 확인.

### 의존성 (10개)
```
fastapi, uvicorn, jinja2, python-multipart
pdfplumber, pymupdf, pillow
openai, httpx, tenacity
python-docx
```
(`python-dotenv`는 스파이크 편의상 추가 설치했으나, `.env` 로드는 `uvicorn`이 아니라 앱 시작 시 직접 처리할지 별도 확인 필요 — Step 1에서 storage/config 설계 시 결정)

### 배포 모델 (★ 설계 전제)

**1차는 개발자 로컬 PC에서 단독 실행합니다. 2차에 사내 서버로 이전합니다.**

```
1차)  본인 PC:  uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1
                 data/ 는 로컬 디스크

2차)  사내 서버 1대 배포 → 전 사용자가 브라우저로 접속, data/ 공유
```

**1차에서 지킬 것**
- `--host 127.0.0.1` 로 바인딩하세요. `0.0.0.0`으로 열지 마세요
- API 키는 `.env`로 관리하고 `.gitignore`에 반드시 추가
- `data/` 경로는 **환경변수 `DATA_DIR`로 주입**(기본값 `./data`). 2차에서 경로만 바꾸면 됩니다

**2차 전환 시 손댈 곳은 3가지뿐입니다:** 바인딩 주소, `DATA_DIR`, API 키 주입 위치.
**그 외 2차용 코드를 지금 만들지 마세요** (사용자 모델, DB 레이어, 권한 데코레이터 전부 금지).

### 캐시의 1차 효용 (오해 방지)

1차는 단독 실행이므로 **"다른 사람이 올린 문서 재사용"은 아직 동작하지 않습니다.** 그래도 캐시는 유지하세요. 1차에서도 다음이 전부 유효합니다.

- 50페이지 문서를 한 번에 다 읽지 않습니다. 다음 날 다시 열면 전부 히트
- 12-18p 번역 후 나중에 19-25p 추가 → 앞부분 그대로 재사용
- 브라우저를 닫거나 서버를 재시작해도 완료분 복구
- **개발 중 같은 PDF를 수십 번 돌립니다** — 이것만으로 본전

구현이 30줄 수준이고 **2차에서 코드 변경 없이 그대로 공유 캐시가 됩니다.**

### Windows 환경 (실사용 전제)

사내 PC는 Windows입니다. 다음을 지키세요.

- 경로는 **`pathlib.Path`만** 사용. 문자열 결합·`/` 하드코딩 금지
- 파일 입출력은 **`encoding="utf-8"` 명시** (Windows 기본은 cp949라 한글이 깨집니다). **콘솔 출력(print/로그)도 마찬가지로 비-ASCII 문자에서 cp949 인코딩 오류가 날 수 있음을 유의** — 서버 로깅은 파일 핸들러 기준 `encoding="utf-8"`을 명시
- 원본 파일명에 한글·공백·괄호가 흔합니다. **저장은 SHA 기준**이고 원래 파일명은 `meta.json`에만 보관하세요 (§2 구조가 이미 이 방식입니다)
- 실행 편의를 위해 **`run.bat`** 을 제공하세요:
  ```bat
  @echo off
  call .venv\Scripts\activate
  start "" cmd /c "timeout /t 5 /nobreak >nul & start http://127.0.0.1:8000"
  uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1
  ```
  브라우저를 uvicorn보다 먼저 열면 바인딩 전이라 "연결 거부"가 잠깐 떴다가 넘어간다
  (실사용 중 발견). 별도 지연 프로세스로 브라우저를 열고, uvicorn은 그대로 foreground에
  둔다(창을 닫으면 서버도 같이 종료되는 기존 동작 유지).
- HTML/DOCX 내보내기 폰트는 **맑은 고딕(Malgun Gothic)** 을 기본으로 지정

---

## 2. 저장 구조 — DB 대신 파일

```
data/
├── glossary.csv                    # 팀 공용 용어집 (엑셀로 직접 편집 가능)
└── docs/
    └── {doc_sha256}/
        ├── source.pdf              # 렌더용 원본
        ├── meta.json               # 파일명, 페이지 수, has_text_layer, 목차
        ├── renders/
        │   └── p012.png
        └── pages/
            └── p012__{key}.json    # 번역 결과 = 캐시. 존재하면 히트
```

**핵심 아이디어: 콘텐츠 주소화(SHA) 저장이 곧 캐시이자 조회입니다.**
동료가 같은 PDF를 다시 업로드하면 SHA가 일치해 `pages/`의 결과를 그대로 읽습니다. **별도 라이브러리·검색 화면이 필요 없습니다.** 재업로드가 곧 조회입니다.

### `meta.json`
```jsonc
{
  "sha256": "a3f9...",
  "filename": "IEC 60502-2 Ed3.pdf",
  "page_count": 120,
  "has_text_layer": true,
  "page_width": 595.276,
  "page_height": 841.89,
  "outline": [{"title": "Annex B", "page": 184}],
  "table_pages": [12, 13, 47],
  "created_at": "2026-08-19T10:00:00+09:00"
}
```

### `glossary.csv`
```csv
en,ko,note
conductor,도체,
sheath,시스,외피 아님 - 사내 표준
armour,외장,
XLPE,가교폴리에틸렌(XLPE),약어 유지
lay-up,연합,
```
- 앱 시작 시 로드 + **파일 mtime 변경 감지 시 자동 리로드** (재시작 불필요)
- 스코프(개인/부서)·승격 워크플로 **없음.** 단일 공용 파일

### 잡 상태
**메모리에만 둡니다** (`dict[job_id]`). 서버 재시작 시 소실되지만, **완료된 페이지 결과는 파일로 남아** 있으므로 다시 열면 캐시 히트로 즉시 복구됩니다. 잡 상태를 파일로 영속화하지 마세요.

---

## 3. 전체 플로우

```
[S1] 업로드
   ├─ SHA-256 산출
   ├─ data/docs/{sha}/ 존재? → meta.json 로드, 렌더 재사용
   └─ 신규 → PDF 파싱, meta.json 생성, 썸네일 렌더
   │
[S2] 페이지 범위 선택
   ├─ 썸네일 + PDF 아웃라인(목차)
   ├─ pages/에 결과 있는 페이지 ✓ 표시
   └─ "8페이지 선택 · 예상 1분 20초 · 신규 5 / 재사용 3"
   │
[S3] 잡 실행 (동시 6)
   │   ├─ 캐시 파일 존재? ── YES ──→ 즉시 SSE push
   │   └─ NO ↓
   ├─ 블록 추출 (텍스트 + bbox, 표/figure 후보 분리 — §5.1)
   ├─ 번역 (해당 페이지 등장 용어만 주입)
   ├─ 후검증 (수치·단위·규격번호·표 구조, 언어 범위 밖은 스킵 — §5.4)
   │      └─ 실패 → 해당 블록만 1회 재번역 → 재실패 시 ⚠
   └─ pages/p012__{key}.json 저장 → SSE push
   │
[S4] 대조 뷰 (완료분부터 실시간 렌더) → 다운로드
```

---

## 4. 블록 스키마 (핵심 계약 — 가장 먼저 확정)

```jsonc
{
  "page_no": 12,
  "page_width": 595.276,      // PDF 포인트
  "page_height": 841.89,
  "render_scale": 2.0,         // PNG px = PDF pt × scale
  "has_text_layer": true,
  "blocks": [
    {
      "id": "p012-b03",                    // 페이지 내 상단→하단 순서
      "type": "paragraph",                 // heading|paragraph|list|table|figure|caption|header_footer
      "bbox": [72.0, 310.5, 523.2, 388.9], // [x0, top, x1, bottom] 좌상단 원점
      "source": "The conductor shall comply with IEC 60228 Class 2.",
      "table": null,
      "ko": "도체(conductor)는 IEC 60228 Class 2를 만족해야 한다.",
      "verify": { "status": "ok", "missing": [], "reason": null }
    },
    {
      "id": "p012-b07",
      "type": "table",
      "bbox": [72.0, 420.0, 523.2, 612.4],
      "source": null,
      "table": {
        "rows": 5, "cols": 4,
        "cells_src": [["Size","Thickness","Current","Weight"], ["1.5","0.7","18","32"]],
        "cells_ko": [["단면적","두께","허용전류","중량"], ["1.5","0.7","18","32"]]
      },
      "ko": null,
      "verify": { "status": "warn", "missing": ["2.5 mm²"], "reason": null }
    },
    {
      "id": "p012-b09",
      "type": "figure",
      "bbox": [72.0, 700.0, 523.2, 800.0],
      "source": null,
      "table": null,
      "ko": null,
      "verify": { "status": "skipped", "missing": [], "reason": "figure - not translated/verified" }
    }
  ]
}
```

**좌표 규약 — 반드시 통일하세요.**
pdfplumber는 **좌상단 원점, y는 아래로 증가**합니다 (`x0, top, x1, bottom`). PyMuPDF도 동일.
브라우저 오버레이 좌표 = `bbox 값 × render_scale` (px). **별도 y축 반전 계산을 넣지 마세요** — 어기면 오버레이가 상하 반전되고 원인 찾는 데 오래 걸립니다. (Step 0 스파이크에서 이 매핑이 정확함을 실측 확인함.)

**`type: header_footer`** — 페이지 상하단 5% 영역. **번역·검증 대상에서 제외**합니다. 페이지 번호 숫자가 검증 오탐을 일으킵니다.

**`type: figure` (v4 신규)** — `find_tables()`가 표로 오탐지했지만 유효성 검사(§5.1 ①)를 통과하지 못한 영역, 즉 그래프·차트·도면 등. **번역·검증 대상에서 제외**하며 `header_footer`와 동일하게 취급합니다. `source`/`table`/`ko`는 모두 `null`. bbox만 기록해 대조 뷰에서 "이 영역은 그림입니다" 표시나 클릭 무시 처리에 사용합니다.

**`verify.status` 값:** `"ok"` | `"warn"` | `"skipped"`. `skipped`는 v4 신규 — §5.4의 언어 범위 밖 숫자 표기 감지 시, 또는 `header_footer`/`figure` 블록에 사용합니다. `reason` 필드에 스킵 사유를 짧게 기록합니다 (예: `"unsupported number format (comma-decimal)"`).

**스캔본 페이지 (`has_text_layer=false`)** — §5.1에서 블록 분해가 불가하므로, 페이지 전체를 **단일 pseudo-block**으로 표현합니다:
```jsonc
{
  "page_no": 40, "has_text_layer": false, "render_scale": 2.0,
  "blocks": [
    {
      "id": "p040-b00",
      "type": "paragraph",
      "bbox": [0, 0, 595.276, 841.89],   // 페이지 전체
      "source": "# 전체 페이지 Markdown...",
      "table": null,
      "ko": "# 번역된 Markdown...",
      "verify": { "status": "skipped", "missing": [], "reason": "scanned page - whole-page markdown, block-level verify not applicable" }
    }
  ]
}
```
ID 집합 검증(§5.3)도 이 경우 단일 ID(`p{page:03d}-b00`)만 확인하면 됩니다. 프론트는 `has_text_layer=false`일 때 블록 클릭 대조를 비활성화하고 스크롤 동기화만 지원합니다(§5.1 안내 문구 참고).

---

## 5. 모듈별 구현 스펙

### 5.1 블록 추출 (`extract/blocks.py`)

**추출 파이프라인 (순서 고정 — Step 0에서 실측 확인된 실패 모드에 대응):**

```
0. page.extract_words() → 단어 + 좌표 (원본, 아직 아무것도 제외 안 함)
1. page.find_tables() → 표 후보 bbox 목록 확보

   ① 표 후보 유효성 검사 (`_is_real_table`, v4.1 갱신)
      아래 조건을 모두 만족해야 "진짜 표"로 인정. 하나라도 걸리면 "가짜 표"(차트/
      그래프의 격자선 오탐)로 판정.
      - 비어있지 않은 셀 비율 ≥ TABLE_MIN_FILLED_RATIO(config, 기본 0.3)
      - 최소 2행×2열, 총 셀 수 ≥ MIN_TABLE_CELLS(config, 기본 6)
      - bbox 크기 ≥ 페이지 실측 기준(PageMetrics.min_table_width/min_table_height,
        아래 ③ 참고) — 라벨 크기의 작은 박스를 걸러냄
      - 5자 이상인 셀이 MIN_LONG_CELLS(config, 기본 2)개 이상 — 숫자만 있는 축
        눈금 박스를 걸러냄
      ※ Step 0에서 막대그래프가 10행×17열 "표"로 오탐지되었고 셀이 전부 빈 값이었음.

   ② 제외된 후보의 처리
      유효성 검사를 통과 못한 후보는 표가 아니라 type="figure" 후보로 재분류.
      (버리지 않음 — bbox는 유지하고 아래 ③④에서 표와 동일하게 제외 영역으로 사용)

   ③ bbox 여유 확장 (표·figure 공통)
      유효한 표 bbox, figure bbox 모두 페이지별 실측 본문 폰트 크기의 배수
      (`PageMetrics.figure_margin` = body_size × 3.2, `blocks.py`의 `measure_page()`가
      페이지당 한 번 계산)만큼 사방으로 확장. 고정 pt 상수(v4의 FIGURE_TABLE_MARGIN_PT)는
      문서마다 폰트 크기가 달라 어떤 문서에선 과하고 어떤 문서에선 부족했다 —
      v4.1에서 문서 실측 배율로 교체. 제목·범례·캡션·축 라벨이 원 bbox 바로 바깥에
      걸치는 경우를 흡수하기 위함. 확장된 영역을 "제외 영역"으로 취급.

   ④ 잔여 텍스트 처리
      ★ 표 영역(확장 후) 내부 단어를 텍스트 블록 후보에서 제외.
      ★ figure 영역(확장 후) 내부 단어도 텍스트 블록 후보에서 제외.
      두 제외를 모두 적용한 뒤 남은 단어만 3~7단계로 넘긴다.
      (이 순서를 지키지 않으면 표 내용이 텍스트 블록으로도 잡혀 중복 번역되거나,
       차트 축 라벨이 의미없는 paragraph 블록으로 새어나간다 — 둘 다 Step 0에서 실측.)

2. 남은 단어를 줄→문단으로 그룹핑
   - 같은 줄 판정은 top 좌표 거리가 아니라 **세로 겹침 비율**(`_same_line`, ≥50%)로
     한다 — 첨자(위/아래첨자)는 폰트 크기가 달라 baseline이 어긋나므로 거리 기준으론
     다른 줄로 잘못 쪼개진다.
   - 문단 경계(줄 간 여백)는 `PageMetrics.paragraph_gap`(body_size × 0.6, 페이지별
     실측)과 비교. 캡션처럼 줄간격이 본문보다 넓은 텍스트가 고정 pt 기준으로
     쪼개지던 문제를 문서 실측 배율로 해결.
   - 첨자 병합(`_merge_superscripts`)도 같은 방식으로 `PageMetrics.superscript_gap`
     (body_size × 0.18)을 사용.
3. 상하단 5% → type=header_footer
4. 폰트 크기가 본문 중앙값의 1.2배 이상 + 짧은 줄 → type=heading
5. id 부여: f"p{page:03d}-b{idx:02d}"  (상단→하단, 좌→우; table/figure 블록도 같은 순번 체계에 포함)
```

**스캔본 처리 (`has_text_layer=false`)**
- bbox를 얻을 수 없으므로 **블록 분해 불가** → 페이지 이미지만 전송, Markdown 통짜 반환
- §4의 단일 pseudo-block 규약을 따름
- **클릭 대조를 비활성화**하고 화면 상단에 명시:
  > "이 문서는 스캔본이라 문장 단위 대조가 제한됩니다. 스크롤 동기화만 지원됩니다."
- 기능을 조용히 실패시키지 마세요.

### 5.2 용어집 매칭 (`glossary.py`)

**LLM 사용 금지. 순수 규칙 기반 + 결정적이어야 합니다** (캐시 키에 들어가므로).

```
1. glossary.csv 로드 (mtime 감지로 자동 리로드)
2. 페이지 텍스트에서 en 항목 매칭
3. 매칭된 항목만 프롬프트에 주입
```

**정규화 규칙**
- 소문자화, 복수형(-s/-es), 하이픈·공백 변형 통일 (`lay-up` = `lay up` = `layup`)
- **긴 용어 우선** (`copper conductor`를 `conductor`보다 먼저 매칭)

전체 용어집을 매번 주입하지 마세요. 토큰 낭비입니다.

> 약어 자동 추출(XLPE, 정의 구문 등)은 **2차 범위**입니다. 1차에서는 구현하지 마세요.

### 5.3 번역 (`translate/engine.py`)

**모델 라우팅** (`config.py` 상수로 분리. Step 0에서 `gpt-5.6-luna`는 실제 API 호출로 검증 완료 — 표 재구성 품질 우수, 행/열 100% 보존, 숫자 손상 0건.)

| 페이지 조건 | 모델 | 입력 |
|---|---|---|
| 표·도면 없음, 텍스트 레이어 있음, 추출 품질 양호 | **`gpt-5.6-luna`** | 블록 텍스트만 |
| 표 또는 figure 포함 | **`gpt-5.6-terra`** | **블록 텍스트 + 페이지 이미지** |
| 표·도면 없어도 추출 품질 저하(`extraction_quality_score` < 0.5) | **`gpt-5.6-terra`** | **블록 텍스트 + 페이지 이미지** |
| 텍스트 레이어 없음(스캔) | **`gpt-5.6-terra`** | 페이지 이미지만 |

**추출 품질 안전망 (v4.1 신규, `choose_model`)** — 고아 문자·PUA 잔여 글리프·짧은
블록 비율로 0~1 점수를 매겨(`extraction_quality_score`), 표·figure가 없는 페이지도
점수가 낮으면 텍스트 전용 모델 대신 페이지 이미지를 함께 보내는 모델로 라우팅한다.
텍스트 추출이 실패에 가까운 페이지를 텍스트만으로 번역해 조용히 품질이 떨어지는
상황을 막기 위함.

Luna는 실측 검증 완료. **`gpt-5.6-terra`/`gpt-5.6-sol`은 아직 실측 검증되지 않았습니다** — 착수 시 실제 호출로 한 번 더 확인하고, 문제가 있으면 config.py 상수만 교체하세요.

**★ `gpt-4o` / `gpt-4o-mini` 를 쓰지 마세요.** 지원 종료(sunset) 예정 모델입니다.

**★ Structured Outputs를 반드시 사용하세요.**
```python
response_format={
  "type": "json_schema",
  "json_schema": {"name": "translation", "strict": True, "schema": {...}}
}
```
Step 0에서 `client.chat.completions.create(..., response_format=...)` 형태로 실제 호출해 정상 동작을 확인했습니다 (openai SDK 3.3.0 기준).
JSON 파싱 실패·코드펜스 혼입 문제가 사라집니다. 다만 **구조만 보장하고 값은 보장하지 않습니다.**
→ `id` 필드의 스키마에 **입력 블록 ID 목록을 `enum`으로 지정**하면 존재하지 않는 ID 생성을 막을 수 있습니다.
→ 그래도 **누락·중복은 막지 못하므로 §5.3 하단의 ID 집합 검증은 그대로 유지**하세요.

**figure/header_footer 블록은 애초에 요청에 포함하지 마세요.** §4/§5.1에서 이미 제외된 블록이므로 LLM에 보낼 필요가 없습니다.

**이미지 입력 형식** (OpenAI 포맷)
```python
{"type": "image_url",
 "image_url": {"url": f"data:image/png;base64,{b64}", "detail": "high"}}
```
표 판독이 목적이므로 `detail: "high"` 를 사용하세요.

**★ 프롬프트 캐싱 — 프롬프트 순서를 지키세요.**
OpenAI의 프롬프트 캐싱은 **접두사(prefix) 자동 매칭**이고, 캐시된 입력은 표준 입력가의 10%로 청구됩니다. 따라서:
```
[고정] 시스템 프롬프트 · 출력 규칙 · JSON 스키마 설명     ← 반드시 앞
[가변] 용어집 매칭 결과 · 블록 텍스트 · 이미지            ← 반드시 뒤
```
순서를 뒤집으면 캐시가 전혀 걸리지 않습니다. 페이지 수십 장을 연속 처리하므로 체감 차이가 큽니다.

**기타**
- 파라미터명은 모델별로 `max_tokens` / `max_completion_tokens` 차이가 있습니다. SDK 최신 버전 문서를 확인하고 `config.py`에서 분기하세요
- Rate limit(429)은 `tenacity`로 지수 백오프 재시도, `Retry-After` 헤더 존중. 동시 6이면 계정 티어에 따라 걸릴 수 있습니다
- 검증 재번역 시 `temperature=0`

**요청 형식**
```jsonc
{
  "doc_title": "IEC 60502-2 Ed.3",
  "terms": [{"en":"sheath","ko":"시스","note":"외피 아님"}],
  "blocks": [{"id":"p012-b03","type":"paragraph","source":"..."},
             {"id":"p012-b07","type":"table","cells_src":[[...]]}]
}
```

**시스템 프롬프트 필수 조항**
1. 입력 블록의 `id`를 **그대로 유지**해 반환. 병합·분할·누락 금지. 번역 불필요 블록도 원문 그대로 담아 반환
2. 수치·단위·규격번호·품번·화학식·상표명 **원형 보존**
3. **단위 변환 절대 금지** (inch→mm 등). 원문 단위 그대로
4. 표는 **행·열 개수 유지**. 셀 병합·분할 금지. 숫자 셀은 번역하지 말고 그대로
5. 용어집이 주어진 용어는 반드시 그 번역어 사용
6. 전문용어는 `도체(conductor)` 형태로 **원문 병기**
7. Structured Outputs 스키마를 따라 응답. 설명·서문을 덧붙이지 말 것
8. 줄바꿈·불릿 기호(•)는 원문 그대로 보존. 목록 항목을 한 줄로 합치지 말 것 (v4.1 신규)

**반환 순서에 의존하지 마세요. 반드시 ID로 매핑하세요.**
표 페이지에 이미지를 함께 넣는 이유는 표 구조 재구성 정확도 차이입니다. 이미지를 참고하되 **반환은 블록 ID 기준**이어야 매핑이 유지됩니다.

**응답 검증 (코드)**
```
returned_ids == input_ids  →  불일치 시 1회 재시도 → 재실패 시 페이지 error
```
**순서에 의존하지 마세요. 반드시 ID로 매핑하세요.**

### 5.4 후검증 (`verify/check.py`)

**전부 규칙 기반입니다. AI에게 검증시키지 마세요.**

**0단계 — 언어 무관 정규화 (항상 적용, 마스킹·추출보다 먼저)**
아래 4가지는 표기 언어와 무관하게 항상 안전하게 적용 가능하므로 순서·범위 제한 없이 먼저 수행합니다.
```
1. 전각 → 반각          （１２３ → 123, ＡＢＣ → ABC)   # 일본 JIS 문서
2. 공백 천단위 제거      (1 234 → 1234)                  # SI/IEC 권장 표기
3. 유니코드 분수 → 소수  (½ → 0.5, ¼ → 0.25 ...)
4. 하이픈 변형 통일      (–, —, − → -)                   # 범위 표기 "30-50℃"
```

**1~4단계 — 마스킹·추출 (0단계 정규화된 텍스트 기준, ★ 순서 고정)**
```
1. 규격번호 마스킹   IEC 60502-2, ASTM B8, EN 50525, KS C 3341, ISO 9001
                    → __SPEC_n__ 치환 후 별도 집합 보관
2. 품번·도면번호 마스킹 (config 패턴) → __PART_n__
3. 단위 동반 수치 추출  "2.5 mm²", "0.6/1 kV", "90 ℃"
4. 나머지 순수 수치 추출
```
1·2를 먼저 하지 않으면 `IEC 60502-2`의 `60502`와 `2`가 수치로 잡혀 원문/번역 집합이 어긋납니다.

**5단계 — 언어 범위 검사 (v4 신규, ★ 1차는 영어 표기로 한정)**
1차 검증 대상은 **영어식 숫자 표기**(`,`=천단위 3자리 그룹, `.`=소수점)로 한정합니다. 4단계에서 추출된 숫자 토큰 각각을 아래 패턴으로 검사:
```
영어식 허용 패턴:  ^-?\d{1,3}(,\d{3})*(\.\d+)?$   (예: 1,234.56 / 2500 / 0.6)
```
블록 내 숫자 토큰 중 **하나라도** 이 패턴에 안 맞으면(예: 독일식 `71,36`의 콤마가 소수점) → **범위 밖으로 판정**:
- 번역은 정상 진행 (이 판정은 검증에만 영향)
- 해당 **블록의** `verify.status = "skipped"`, `reason = "unsupported number format (non-English decimal/thousand notation)"`
- 값 비교(2단계 이하)는 **수행하지 않음** — 조용히 틀리기보다 "검증 안 했다"고 명시하는 것이 안전
- 화면에는 검증 경고(⚠)와 별도로 **"⏭ 검증 생략 N건 (미지원 숫자 표기)"** 카운터로 집계 (§5.4 실패 처리, §7.3 참고)

> 다국어 숫자 표기 전면 지원(독일식 콤마 소수점, 인도식 자릿수 구분 등)은 **2차 범위**입니다. 1차에서 언어별 분기 로직을 만들지 마세요 — 위 스킵 처리로 충분합니다.

**정규화 (영어 범위 검사 통과 시에만 적용)**
- 천단위 콤마 제거 (`2,500` → `2500`)
- 단위 표기 통일 (`mm²`=`mm2`=`sq.mm`, `℃`=`°C`=`degC`)

**비교**
- 유형별 **다중집합(multiset)** 비교 — 순서 무관, **개수 일치**까지
- 표는 별도로 `rows`·`cols`·셀 개수 일치 검사
- 줄바꿈(`\n`) 개수가 원문·번역 간에 다르면 `missing`에 "줄바꿈 구조 불일치" 추가
  (v4.1 신규 — §5.3 시스템 프롬프트 8번 조항 위반 감지)
- `type=header_footer`, `type=figure` 블록은 **검증 제외**

**실패 처리**
```
1차 실패 → 해당 블록만 재번역 (누락 항목 명시적 나열, temperature=0)
2차 실패 → verify.status="warn", missing 저장
           화면: 블록 하이라이트 + ⚠ 배지
           툴팁: "원문의 '2.5 mm²'가 번역에서 확인되지 않습니다"
언어 범위 밖 → verify.status="skipped" (재번역 시도하지 않음 — 값 비교 자체가 불가하므로)
```
**조용히 넘기지 마세요.** 화면 상단에 `검증 경고 3건 · 검증 생략 2건` 요약 + 클릭 시 해당 블록으로 스크롤.

### 5.5 캐시 (`cache.py`)

DB 없이 **파일 존재 여부**로 동작합니다.

```python
terms_hash = sha256(canonical_json(sorted(matched_terms_for_this_page)))[:12]
key = sha256(f"{terms_hash}|{PROMPT_VER}|{model_id}|ko")[:16]
path = f"data/docs/{doc_sha}/pages/p{page:03d}__{key}.json"

if path.exists(): return json.load(path)   # HIT
```

**키 구성요소의 이유 (하나라도 빼면 버그)**
- `terms_hash` — 용어집을 고쳤는데 옛 번역이 나오면 신뢰를 잃습니다. **그 페이지에 실제 주입된 용어 집합**의 해시를 쓰세요
- `PROMPT_VER` — 프롬프트 개선이 반영 안 되면 디버깅이 지옥이 됩니다. `prompts.py` 상단에 상수로 관리
- `model_id` — 모델이 바뀌면 결과가 달라집니다

**★ 원자적 쓰기.** 1차 단독 실행에서도 잡 취소·강제 종료 시 **쓰다 만 JSON이 남으면 이후 계속 파싱 에러**가 납니다. 2차 서버 배포에서는 동시 기록까지 방어합니다.
```python
tmp = path.with_suffix(".json.tmp")
tmp.write_text(json.dumps(data, ensure_ascii=False), encoding="utf-8")
tmp.replace(path)          # os.replace = 원자적. Windows에서도 동작
```
읽을 때는 `json.JSONDecodeError`를 캐시 미스로 처리하고 재번역하세요. 락은 걸지 마세요 — 결과가 동일하므로 last-write-wins로 충분합니다.

**만료 없음.** 1차에서는 정리 로직을 만들지 마세요. 필요하면 디렉터리를 수동 삭제합니다.
서버 디스크 증가는 텍스트 JSON + PNG 기준으로 문서당 수십 MB 수준이므로 1차에서는 문제되지 않습니다.

---

## 6. 잡 + 스트리밍

### 엔드포인트
```
POST /api/jobs                     → {job_id}, 백그라운드 시작
GET  /api/jobs/{job_id}/stream     → SSE
GET  /api/docs/{sha}/pages?range=  → 저장된 결과 조회 (새로고침·재접속용)
POST /api/jobs/{job_id}/cancel
```

**새로고침 시 잡을 조회하지 말고 `pages/` 파일을 읽으세요.** 잡은 메모리에만 있고, 결과는 파일에 있습니다. 이 분리가 상태 관리를 크게 단순화합니다.

### SSE 이벤트
```jsonc
{"type":"page_done","page":13,"blocks":[...],"verify":"warn","cached":true}
{"type":"progress","done":13,"total":48,"eta_sec":95}
{"type":"page_error","page":21,"reason":"..."}
{"type":"job_done","warn_count":3,"skipped_count":2}
```

### 구현 주의

**① 순서** — 병렬이라 완료 순서가 뒤섞입니다. 화면은 **선택 페이지 순서대로 스켈레톤을 미리 깔고**, 도착하는 대로 자리를 채우세요. 도착순 append 시 p.15가 p.12 위에 뜹니다.

**② 검증 전 노출 금지** — `page_done`은 **검증(재번역 포함) 완료 후**에만 push. 재번역 중에는 `verifying` 상태 표시. 검증 안 된 결과가 먼저 뜨면 사용자가 그걸 복사해 갑니다.

**③ ★ 워커 수** — 잡 레지스트리가 인메모리라 **워커 2개 이상이면 깨집니다.** 잡이 워커1에서 시작됐는데 SSE가 워커2에 연결되면 이벤트가 전혀 안 옵니다.
→ **`uvicorn --workers 1` 고정.** README에 명시.

**④ 브라우저를 닫아도 잡은 계속 진행**되어야 합니다. FastAPI `BackgroundTasks`는 요청과 함께 종료되므로 사용 금지. `asyncio.create_task` + 잡 레지스트리로 구현.

**⑤ 동시성** — `asyncio.Semaphore(6)`. 캐시 히트 페이지는 세마포어를 소모하지 않고 즉시 push.

**⑥ 재연결** — SSE `Last-Event-ID` 지원. 실패 시 `GET /api/docs/{sha}/pages`로 전체 재조회 후 재구독.

---

## 7. 화면 (3개)

### 7.1 업로드 (`/`)
- 드래그앤드롭, PDF만, 최대 100MB
- 업로드 → SHA 계산 → 기존 디렉터리 존재 시 **"이 문서는 이전에 번역된 적이 있습니다. 결과를 재사용합니다."**
- 하단에 **최근 문서 목록** — `data/docs/*/meta.json` 디렉터리 스캔, 최근 10개, 파일명 + 번역된 페이지 수. 클릭 시 바로 대조 뷰
  > 검색·페이지네이션은 만들지 마세요. 단순 목록이면 충분합니다.
  > 1차는 본인 PC의 문서만 보입니다. 2차 서버 배포 시 자동으로 팀 공용 목록이 됩니다 (코드 변경 불필요).

### 7.2 범위 선택 (`/doc/{sha}/select`)
- **썸네일 그리드** (폭 150px). 표 포함 페이지에 `표` 배지, **결과가 있는 페이지에 `✓` 배지**
- **PDF 아웃라인 기반 목차** — "Annex B (p.184-201)" 원클릭 선택. IEC/ASTM은 대부분 아웃라인 보유
- 입력 문법: `12-18, 25, 40-47`
- **기본값은 전체 선택이 아니라 미선택 상태**
- 선택 즉시: `8페이지 선택 · 예상 1분 20초 · 신규 5 / 재사용 3`

### 7.3 대조 뷰 (`/doc/{sha}/view?range=12-18`)
```
┌──────────────────────┬──────────────────────┐
│ 원문 (PNG + 오버레이) │ 번역 (블록 단위)      │
│  ┌────────────────┐  │  ┌────────────────┐  │
│  │ 투명 div        │──┼─▶│ 하이라이트+스크롤 │  │
│  │ data-block-id   │  │  │ data-block-id   │  │
│  └────────────────┘  │  └────────────────┘  │
│  [p.12]              │  [p.12]           ⚠  │
└──────────────────────┴──────────────────────┘
상단: 진행률 · 검증 경고 N건 · 검증 생략 N건 · [다운로드]
```
- **양방향 클릭.** 어느 쪽을 눌러도 반대편이 하이라이트되고 뷰포트로 스크롤
- 오버레이 div는 `position:absolute`, 좌표 = `bbox × render_scale`, 기본 `transparent`, hover 시 옅은 배경
- `type=figure` 블록은 오버레이를 아예 그리지 않거나(클릭 대상에서 제외) 옅은 회색 테두리로만 표시
- `verify.status=="warn"` 블록은 우측에 ⚠ + 툴팁. `verify.status=="skipped"` 블록은 ⏭ + 툴팁("숫자 표기 미지원으로 검증 생략, 번역은 정상 수행됨")
- **미번역 페이지 인라인 추가** — 스크롤 중 미번역 페이지에서 `이 페이지 번역` 버튼
- 스캔본은 클릭 대조 비활성 + 안내 문구 (§5.1)
- 다운로드: **HTML(대조본)** / **DOCX(번역본만, python-docx)**

> 용어 등록 UI는 **2차 범위**입니다. 1차에서는 `data/glossary.csv`를 직접 편집합니다. README에 파일 위치와 편집법을 안내하세요.

---

## 8. 프로젝트 구조

```
.
├── app/
│   ├── main.py                 # FastAPI 엔트리
│   ├── config.py                # 패턴·모델명·상수 (TABLE_MIN_FILLED_RATIO 등, 하드코딩 금지).
│   │                             #   페이지 크기에 스케일해야 하는 임계치(문단 간격·표 최소
│   │                             #   크기·figure 마진·첨자 간격)는 여기 고정 상수 대신
│   │                             #   extract/blocks.py의 PageMetrics/measure_page()가 담당 (v4.1)
│   ├── storage.py               # data/ 경로 규약, meta.json 입출력
│   ├── cache.py                 # 캐시 키 + 파일 조회/저장
│   ├── glossary.py              # CSV 로드·mtime 리로드·매칭
│   ├── extract/
│   │   ├── blocks.py            # bbox 블록 추출 (표/figure 후보 검증·마진 확장 포함, §5.1)
│   │   ├── render.py            # PyMuPDF 페이지 PNG
│   │   └── outline.py           # PDF 목차
│   ├── translate/
│   │   ├── engine.py            # 라우팅·병렬·재시도
│   │   ├── prompts.py           # PROMPT_VER 상수와 함께 관리
│   │   └── client.py            # OpenAI 래퍼, JSON 파싱
│   ├── verify/check.py          # 0단계 언어무관 정규화 + 영어범위 검사(§5.4) 포함
│   ├── jobs.py                  # 인메모리 레지스트리 + asyncio + SSE
│   ├── export.py                # HTML / DOCX 생성
│   ├── routers/
│   └── templates/  static/
├── data/
│   ├── glossary.csv
│   └── docs/
├── tests/
├── requirements.txt
├── .env.example
└── README.md
```

**`models.py`, `auth/`, `db.py`를 만들지 마세요.** 1차에는 없는 개념입니다.

---

## 9. 성능 감각 (참고)

50페이지 기준: 입력 6~8만 토큰, 출력 5~7만 토큰, 동시 6 처리 시 2~4분.
캐시 히트 문서는 1초 내 전체 렌더.

---

## 10. 구현 순서

| Step | 내용 | 산출 | 상태 |
|---|---|---|---|
| **0** | **스파이크** — 실제 PDF 1페이지(표 포함)로 ⓐ 표 재구성 품질 ⓑ bbox 좌표 정확도 ⓒ **표-텍스트 중복 발생 여부** 확인 | **결과 보고 후 대기** | ✅ 완료 (§0-A) |
| 1 | 블록 스키마(§4) + `storage.py` 경로 규약 확정 | | 🔄 진행 중 (스키마 확정, storage.py 남음) |
| 2 | 단일 페이지 파이프라인 CLI — 추출→번역→검증 | `python -m app.cli --pdf x.pdf --page 12` | |
| 3 | 대조 뷰 HTML (Mock 데이터) — 오버레이 클릭 동작까지 | 결과물 모양 확정 | |
| 4 | 용어집 CSV + 매칭 | | |
| 5 | 캐시 (파일 기반) | | |
| 6 | 잡 + 병렬 + SSE | | |
| 7 | 업로드/범위선택 화면 + 다운로드 | | |

**Step 0에서 실패가 나오면 코드를 더 쓰지 말고 설계를 재검토합니다.** (→ 실제로 ⓒ에서 새로운 실패 모드가 나와 §4/§5.1/§5.4를 v4로 수정함)
**Step 3까지 하면 데모가 가능합니다.** 시간이 부족하면 DOCX 내보내기를 빼세요. **검증(2)과 범위 선택은 빼면 안 됩니다.**

---

## 11. 하지 말아야 할 것

**1차 범위 밖**
- ❌ 로그인·세션·사용자 개념
- ❌ DB·ORM·마이그레이션 (SQLite 포함)
- ❌ 보안 스캔·문서 등급·차단 게이트·보안팀 통보
- ❌ 용어 스코프(개인/부서)·승격 워크플로·등록 UI
- ❌ 약어 자동 추출
- ❌ 라이브러리 검색·통계·사용 로그
- ❌ **2차용 스텁·추상화 레이어 선반영**
- ❌ 다국어 숫자 표기 전면 지원 (언어별 정규화 분기) — 범위 밖은 검증 스킵으로 대응

**구현 함정**
- ❌ `gpt-4o` / `gpt-4o-mini` 사용 (지원 종료 예정)
- ❌ Structured Outputs 미사용 (JSON 파싱 실패의 주원인)
- ❌ Structured Outputs를 썼다고 **ID 집합 검증을 생략** (구조만 보장, 값은 미보장)
- ❌ 가변 내용(용어집·블록·이미지)을 프롬프트 앞에 배치 (프롬프트 캐싱 무효화)
- ❌ `--host 0.0.0.0` 으로 로컬 실행 (1차는 `127.0.0.1`)
- ❌ `encoding="utf-8"` 생략 (Windows cp949로 한글 깨짐; 콘솔 출력도 동일 함정)
- ❌ 표 영역 단어를 텍스트 블록에서 제외하지 않음 (중복 번역)
- ❌ **표 후보를 무조건 신뢰** (차트/그래프가 표로 오탐지될 수 있음 — §5.1 ① 유효성 검사 필수)
- ❌ 규격번호 마스킹 전에 수치 추출 (검증 오탐 폭증)
- ❌ 콤마를 무조건 천단위로 정규화 (비영어 소수점 콤마 표기 손상 — §5.4 5단계로 대응)
- ❌ 블록을 **순서**로 매핑 (반드시 ID)
- ❌ 검증 완료 전 결과 push
- ❌ `uvicorn --workers 2` 이상 (SSE 유실)
- ❌ 잡 상태를 파일로 영속화 (결과만 파일, 잡은 메모리)
- ❌ AI로 숫자 검증 / AI로 용어 추출
- ❌ 문장 단위 정렬 시도 (한↔영 1:1 미성립 → **블록 단위**)
- ❌ 원문 PDF 위 번역 오버레이(덧씌우기) — 길이 차이·폰트 문제로 시간 대비 효용 최악
- ❌ `config` 없이 패턴·모델명·임계치(TABLE_MIN_FILLED_RATIO 등) 하드코딩. 페이지 크기에
     비례해야 하는 임계치를 고정 pt 상수로 되돌리지 말 것 — PageMetrics로 이관한 이유임(부록 참고)

---

## 부록: v3→v4 변경 이력

Step 0 스파이크(§0-A, samplePDF.pdf 실측)를 근거로 다음을 수정:

1. **§8 `client.py` 주석 오기 수정**: "Anthropic 래퍼" → "OpenAI 래퍼" (§1 확정 스택은 OpenAI API이며 `anthropic` 의존성 자체가 없었음. v3의 잔재 문구로 확인).
2. **§4 블록 타입에 `figure` 추가**, `verify.status`에 `"skipped"` 추가, `reason` 필드 추가. 스캔본 페이지의 blocks 배열 표현(단일 pseudo-block)을 명문화.
3. **§5.1 추출 파이프라인에 ①②③④ 단계 추가**: 표 후보 유효성 검사 → 무효 후보의 figure 재분류 → 표/figure 공통 bbox 마진 확장 → 잔여 텍스트 제외. (근거: 차트가 표로 오탐지되고 축 라벨이 새어나가는 실패 모드를 Step 0에서 실측.)
4. **§5.4 정규화를 2단계로 분리**: 0단계(언어 무관, 항상 적용 — 전각/반각, 공백천단위, 유니코드분수, 하이픈통일) + 5단계(언어 범위 검사 — 1차는 영어식 표기만 검증 대상, 범위 밖은 번역은 진행하되 해당 블록만 검증 스킵). (근거: 샘플 문서의 독일식 콤마-소수점 표기가 기존 "천단위 콤마 제거" 규칙과 충돌해 수치를 파괴함을 실측.)
5. **§1에 Python 버전 재확인 메모 추가** (로컬 3.14.6 vs 스펙 3.11).
6. **§5.3 모델 검증 상태 명시**: `gpt-5.6-luna`만 실제 API 호출로 검증 완료. `terra`/`sol`은 착수 시 재확인 필요.

## 부록: v4→v4.1 변경 이력

실사용 중 발견한 문제들을 근거로 다음을 수정 (구현 완료, 코드 기준 문서 동기화):

1. **§5.1 표 유효성 검사 강화**: 셀 채움 비율만으로는 라벨 크기의 작은 박스·숫자만 있는
   축 눈금 박스가 표로 오탐지됨 → 최소 행/열·셀 수·bbox 크기·긴 텍스트 셀 개수 조건 추가
   (`_is_real_table`).
2. **§5.1/§8/§11 임계치를 고정 pt 상수에서 페이지 실측 배율로 전환**: 문서마다 폰트
   크기가 달라 고정 pt(문단 간격 6pt, figure 마진 36pt, 표 최소 크기 120×40pt, 첨자
   간격 1.5pt)가 어떤 문서에선 과하고 어떤 문서에선 부족했음 → `blocks.py`의
   `PageMetrics`/`measure_page()`가 페이지당 본문 폰트 크기·줄간격을 실측해 배율로 계산.
3. **§5.1 같은 줄 판정을 top거리에서 세로 겹침 비율로 전환**: 첨자는 폰트 크기가 달라
   baseline이 달라 거리 기준으로는 별도 줄로 잘못 분리됨(예: "ϑ0"의 아래첨자) →
   `_same_line`이 세로 겹침 비율(≥50%)로 판정.
4. **§5.3 시스템 프롬프트 8번 조항 추가**: 줄바꿈·불릿 기호 보존, 목록 항목 병합 금지.
   `PROMPT_VER`를 v3→v4로 올려 캐시 무효화.
5. **§5.3 모델 라우팅에 추출 품질 안전망 추가**: 표·figure가 없어도 텍스트 추출 품질이
   낮으면(`extraction_quality_score` < 0.5) 텍스트 전용 모델 대신 페이지 이미지를 함께
   보내는 모델로 라우팅 (`choose_model`).
6. **Windows §run.bat 브라우저 실행 순서 수정**: `start http://...`가 `uvicorn` 시작보다
   먼저 실행돼 포트 바인딩 전에 브라우저가 열려 "연결 거부"가 잠깐 노출됨 → 브라우저
   오픈을 지연된 별도 프로세스로 분리.
7. **클라이언트 수식 크롭 URL을 서버 라우트 계약에 맞춤**: `view.js`가 경로 세그먼트로
   크롭 좌표를 보냈으나 서버(`routers/docs.py`)는 쿼리 파라미터(`x0/top/x1/bottom`)를
   받음 → 클라이언트를 서버 계약에 맞춰 수정 (서버는 그대로 유지).
8. **§5.4 검증에 줄바꿈 구조 검사 추가**: 8번 조항(줄바꿈·불릿 보존)이 지켜졌는지
   `source`/`ko`의 `\n` 개수를 비교해 다르면 경고 (`check_text_block`).
9. **§5.1 불릿 줄바꿈 유실 버그 수정**: `_join_lines()`의 불릿 판정(`_BULLET_RE`)이
   Symbol 폰트 PUA 글리프 복원보다 먼저 실행돼, PDF의 불릿이 Symbol 폰트로 인코딩된
   경우 원시 PUA 코드를 보고 판정에 실패해 줄바꿈 없이 공백으로 합쳐짐 → PUA 복원을
   줄 단위로 앞당겨 해결 (`_build_line_info`). 전체 샘플 문서(85p) 재스캔으로 검증.
10. **§4 도식형 figure도 크롭 이미지로 표시**: 벡터 클러스터 bbox(도식)는 텍스트
    placeholder("[그림 - 좌측 원문 참조]") 대신 수식과 동일하게 크롭 이미지를 보여줌
    (사용자 요청). bbox가 다소 부정확할 수 있는 트레이드오프는 유지.
11. **§5.3 8번 조항에 예시 추가 + 재번역 안내 강화**: 추상적 지시보다 구체적 before/after
    예시가 LLM 준수율이 높다는 판단으로 "• Item one\n• Item two" → "• 항목 하나\n•
    항목 둘" 예시 추가. `retranslate_block()`도 "줄바꿈 구조 불일치" 재번역 시
    "verbatim" 문구 대신 목표 `\n` 개수를 직접 명시. `PROMPT_VER`를 v5→v6으로 올림.
